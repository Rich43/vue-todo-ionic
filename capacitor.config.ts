import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Vue Todo Ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
